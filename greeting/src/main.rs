/// # Documents comments ： sum function
/// This function is used to sum staff
/// #usage:
/// assert_eq!(3, sum(1,2));

struct Empty;
fn main() {
    let x = Empty;
    println!("{:p}", &x);
    let y = x;
    println!("{:p}", &y);
    let z = Empty;
    println!("{:p}", &z);
    assert_eq!((..), std::ops::RangeFull);
}
